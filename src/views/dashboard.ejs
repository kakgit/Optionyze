<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/css/app-shell.css" />
    <link rel="stylesheet" href="/css/account-menu.css" />
    <link rel="stylesheet" href="/css/dashboard.css" />
</head>
<body class="app-page">
    <%
        const vFullName = String(currentAccount?.fullName || "User").trim();
        const vInitial = vFullName ? vFullName.charAt(0).toUpperCase() : "U";
        const vIsAdmin = Boolean(currentAccount?.isAdmin);
        const vMustChangePassword = Boolean(currentAccount?.mustChangePassword);
        const vRoleLabel = vIsAdmin ? "Administrator" : "Standard User";
    %>
    <div class="app-layout">
        <%- include('partials/app-header', { currentAccount }) %>

        <section class="dashboard-hero">
            <div class="app-panel">
                <div class="dashboard-eyebrow">Control Center</div>
                <h1 class="dashboard-title">Build, monitor, and grow your trading workspace from one dashboard.</h1>
                <p class="app-copy">This is now the main landing page for your account. Use it to move into strategy controls, tighten account security, and keep the server-side setup organized as the product grows.</p>
                <div class="dashboard-actions">
                    <a class="app-solid-btn" href="/strategyfogreeks">Open StrategyFOGreeks</a>
                    <a class="app-ghost-btn" href="/account/profile">Review My Profile</a>
                    <a class="app-ghost-btn" href="/account/delta-exchange-api">Delta API Settings</a>
                </div>
                <% if (vMustChangePassword) { %>
                    <div class="dashboard-note">Your account is marked for a password update. Please change it before doing anything important in the platform.</div>
                <% } else { %>
                    <div class="dashboard-note">Your login session is active and the dashboard is ready for the next phase of product buildout.</div>
                <% } %>
                <% if (infoMessage) { %>
                    <div class="app-success"><%= infoMessage %></div>
                <% } %>
            </div>

            <div class="dashboard-side">
                <div class="dashboard-profile">
                    <div class="dashboard-avatar-row">
                        <div class="dashboard-avatar"><%= vInitial %></div>
                        <div>
                            <div class="dashboard-name"><%= vFullName %></div>
                            <div class="dashboard-meta"><%= currentAccount?.email || "No email available" %></div>
                        </div>
                    </div>
                    <div class="dashboard-chips">
                        <span class="dashboard-chip <%= vIsAdmin ? 'dashboard-chip-admin' : 'dashboard-chip-user' %>"><%= vRoleLabel %></span>
                        <span class="dashboard-chip dashboard-chip-ready">Server Connected</span>
                        <% if (vMustChangePassword) { %>
                            <span class="dashboard-chip dashboard-chip-password">Password Change Required</span>
                        <% } %>
                    </div>
                </div>
                <div class="dashboard-mini-grid">
                    <div class="app-stat dashboard-stat">
                        <div class="dashboard-stat-label">Mobile</div>
                        <div class="dashboard-stat-value"><%= currentAccount?.mobileNo || "--" %></div>
                    </div>
                    <div class="app-stat dashboard-stat">
                        <div class="dashboard-stat-label">Access</div>
                        <div class="dashboard-stat-value"><%= vIsAdmin ? "Full" : "User" %></div>
                    </div>
                    <div class="app-stat dashboard-stat">
                        <div class="dashboard-stat-label">Security</div>
                        <div class="dashboard-stat-value"><%= vMustChangePassword ? "Pending" : "Ready" %></div>
                    </div>
                    <div class="app-stat dashboard-stat">
                        <div class="dashboard-stat-label">Primary Workspace</div>
                        <div class="dashboard-stat-value">Paper</div>
                    </div>
                </div>
            </div>
        </section>

        <section class="dashboard-section">
            <div class="dashboard-section-head">
                <div>
                    <h2>Quick Actions</h2>
                    <p class="app-copy">Use these entry points to move between account setup, strategy control, and team administration without digging through menus.</p>
                </div>
            </div>
            <div class="dashboard-shortcut-grid">
                <a class="dashboard-shortcut" href="/strategyfogreeks">
                    <div class="dashboard-shortcut-kicker">Strategy</div>
                    <div class="dashboard-shortcut-title">Launch Paper Runner</div>
                    <div class="dashboard-shortcut-copy">Open the StrategyFOGreeks control surface and continue from the live server-backed paper workflow.</div>
                </a>
                <a class="dashboard-shortcut" href="/account/profile">
                    <div class="dashboard-shortcut-kicker">Profile</div>
                    <div class="dashboard-shortcut-title">Update Personal Details</div>
                    <div class="dashboard-shortcut-copy">Review your name, email, mobile number, and Telegram details so account ownership stays clean.</div>
                </a>
                <a class="dashboard-shortcut" href="/account/delta-exchange-api">
                    <div class="dashboard-shortcut-kicker">Credentials</div>
                    <div class="dashboard-shortcut-title">Manage Delta API Profiles</div>
                    <div class="dashboard-shortcut-copy">Store and organize exchange API references before strategy automation expands further.</div>
                </a>
                <% if (vIsAdmin) { %>
                    <a class="dashboard-shortcut" href="/mngusers">
                        <div class="dashboard-shortcut-kicker">Admin</div>
                        <div class="dashboard-shortcut-title">Manage Users</div>
                        <div class="dashboard-shortcut-copy">Create accounts, control admin rights, and manage password reset status from the PostgreSQL-backed admin workspace.</div>
                    </a>
                <% } %>
                <a class="dashboard-shortcut" href="/account/change-password">
                    <div class="dashboard-shortcut-kicker">Security</div>
                    <div class="dashboard-shortcut-title">Change Password</div>
                    <div class="dashboard-shortcut-copy">Rotate your password now, especially if you are still using a temporary bootstrap or reset credential.</div>
                </a>
                <a class="dashboard-shortcut" href="/">
                    <div class="dashboard-shortcut-kicker">Platform</div>
                    <div class="dashboard-shortcut-title">Return to Home</div>
                    <div class="dashboard-shortcut-copy">Jump back to the public landing page while the dashboard continues evolving into the core signed-in workspace.</div>
                </a>
            </div>
        </section>

        <section class="dashboard-section dashboard-timeline">
            <div class="dashboard-timeline-card">
                <div class="dashboard-section-head">
                    <div>
                        <h2>Suggested Flow</h2>
                        <p class="app-copy">This is the cleanest sequence for getting an account production-ready without skipping setup details that will matter later.</p>
                    </div>
                </div>
                <div class="dashboard-steps">
                    <div class="dashboard-step">
                        <div class="dashboard-step-num">1</div>
                        <div>
                            <div class="dashboard-step-title">Secure the account</div>
                            <p class="app-copy">Confirm your profile data and set a strong password so the account is stable before strategy usage ramps up.</p>
                        </div>
                    </div>
                    <div class="dashboard-step">
                        <div class="dashboard-step-num">2</div>
                        <div>
                            <div class="dashboard-step-title">Configure API references</div>
                            <p class="app-copy">Add Delta Exchange API profiles and keep them organized by reference name instead of scattering keys across sessions.</p>
                        </div>
                    </div>
                    <div class="dashboard-step">
                        <div class="dashboard-step-num">3</div>
                        <div>
                            <div class="dashboard-step-title">Enter the strategy workspace</div>
                            <p class="app-copy">Use StrategyFOGreeks paper mode as the current operating area while the broader platform dashboard continues to expand.</p>
                        </div>
                    </div>
                    <% if (vIsAdmin) { %>
                        <div class="dashboard-step">
                            <div class="dashboard-step-num">4</div>
                            <div>
                                <div class="dashboard-step-title">Provision the team</div>
                                <p class="app-copy">From Manage Users, create new accounts, assign admin access carefully, and keep reset flows intentional.</p>
                            </div>
                        </div>
                    <% } %>
                </div>
            </div>

            <div class="dashboard-timeline-card">
                <div class="dashboard-section-head">
                    <div>
                        <h2>Workspace Notes</h2>
                        <p class="app-copy">A few useful signals about the account and where this dashboard sits in the product right now.</p>
                    </div>
                </div>
                <div class="dashboard-detail-list">
                    <div class="dashboard-detail">
                        <strong>Current role</strong>
                        <span><%= vIsAdmin ? "You can access both strategy workflows and admin management tools." : "You currently have access to user-level profile, security, and strategy areas." %></span>
                    </div>
                    <div class="dashboard-detail">
                        <strong>Account email</strong>
                        <span><%= currentAccount?.email || "No email configured." %></span>
                    </div>
                    <div class="dashboard-detail">
                        <strong>Platform state</strong>
                        <span>The app is live on Railway and this dashboard is now positioned as the signed-in home base for future status, PnL, runner health, and account summaries.</span>
                    </div>
                    <div class="dashboard-detail">
                        <strong>Next dashboard milestone</strong>
                        <span>Add live runner stats, strategy summaries, account completion indicators, and server activity snapshots once the backend endpoints are ready.</span>
                    </div>
                </div>
                <% if (vIsAdmin) { %>
                    <div class="dashboard-info-banner">Admin visibility is enabled for this account, so this dashboard can become the control room for user provisioning and future operational monitoring.</div>
                <% } %>
            </div>
        </section>
    </div>
</body>
</html>
